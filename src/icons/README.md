Pasta de icones SVG locais do `astro-icon`.

Existe vazia so para silenciar o aviso de build. Os icones do site vem do
conjunto Lucide (`@iconify-json/lucide`), usados como `lucide:nome`.

Para adicionar um icone proprio: solte `meu-icone.svg` aqui e use
`<Icon name="meu-icone" />`.
